# M5 性能計測 (2026-07-04)

環境: Apple Silicon Mac (Darwin 25.5) / Python 3.14.6 / `/usr/bin/time -p` による実測。
キャッシュ済み zip 使用 (ネットワーク時間を含まない)。コマンドは `gtfs-semdiff compare`。

| フィード | trips (旧/新) | RawDiff | イベント | 実時間 |
|---|--:|--:|--:|--:|
| 永井運輸 prev_2→prev_1 | 421 / 491 | 14,399 | 95 | 1.17 s |
| 富山地鉄バス prev_2→prev_1 (R8 改正) | 1,399 / 1,354 | 30,700 | 282 | 1.89 s |
| 臨港バステスト ダイヤ01→系統路線増減 | 2,866 / 2,828 | 5,051 | 4 | 1.15 s |

DoD (目安 5 分以内) に対して全ペア 2 秒以内。現時点で最適化は不要
(CLAUDE.md 原則 5: 早すぎる最適化の禁止 — 本計測はその判断根拠の記録)。

計算量メモ (将来大規模フィードで問題になった場合の当たり):
- diff0: 行数線形 (dict 突合)
- identity パターンクラスタリング: family 内 O(patterns²)、50 超で予備グループ化
- shapes 離散 Fréchet: shape あたり O(200²) (max_polyline_points で間引き)
