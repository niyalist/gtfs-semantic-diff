# W1 検証ログ: 結果バンドル + HTML ビューア

実施日: 2026-07-04 / 出力: `data/m8_{nagai,chitetsu,rinko}_report.html`

## 生成結果

| フィード | イベント | 単一 HTML サイズ | 地図対象イベント | 時刻表対象 |
|---|--:|--:|--:|--:|
| 永井運輸 (prev_2→prev_1) | 95 | 4.0 MB | 33 | 71 |
| 富山地鉄 R8 (prev_2→prev_1) | 282 | 7.6 MB | 124 (shape線 176本) | — |
| 臨港テスト (系統路線増減) | 4 | 1.8 MB | 0 | 0 |

コマンド: `gtfs-semdiff compare ... --html out.html` (自己完結・単一ファイル)。

## 構造検証 (自動)

- pytest 118 件通過 (bundle 構造 / geometry ステータス / 時刻表カバレッジ /
  HTML 埋め込み+エスケープ / CLI --html の6件を追加)
- node スモークテスト: 3フィードの実バンドルに対し、**全イベント** (95+282+4) の
  ja/en 整形・subject ラベル生成が例外なく完了し、**全 evidence ID が rawdiff 索引で
  解決できる** (「全ての変化をクリックすれば生値が出る」の構造保証) ことを確認
- ビューアは Svelte 4 + Vite。ビルド成果物 942KB (gzip 252KB) を
  `src/gtfs_semdiff/report/viewer_template.html` としてパッケージに同梱
  (再ビルド: `scripts/build_viewer.sh`)

## ビューア構成

概要 (サマリ + major 一覧) → route_group 章 (折りたたみ、major を含む章は初期展開) →
変更のない路線 → 停留所の変更 → データ検証 (explained_ratio・残差全件)。
各イベント行クリックで詳細パネル: 数値詳細 / 区間別所要時間表 / 停車パターン差分
(追加=緑・削除=赤取り消し線) / 発車時刻表 before/after (時単位グリッド、増減ハイライト) /
MapLibre 地図 (地理院タイル、停留所異動の色分け・shape 新旧の重ね描き) / evidence 生値表。
言語切替 (日本語/EN) はヘッダ右上。

## DoD 状況

- [x] 3検証フィードで HTML 生成、全イベントの evidence 解決 (構造検証)
- [x] 単一 HTML ファイルとして保存可能 (自己完結、地図タイルのみオンライン)
- [ ] **ブラウザでの目視確認 (開発者の確認待ち)**: `data/m8_*_report.html` を開き、
  クリックドリルダウン・地図描画・時刻表表示・言語切替を確認する

目視確認で問題が見つかった場合は本ログに追記して修正する。
